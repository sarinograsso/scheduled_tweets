<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to("Scheduled Tweets", root_path, class: "navbar-brand") %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to("Home", root_path, class: "nav-link #{'active' if current_page?(root_path)}") %>
        <%= link_to("About", about_path, class: "nav-link #{'active' if current_page?(about_path)}") %>
        <%= link_to("Twitter Accounts", twitter_accounts_path, class: "nav-link #{'active' if current_page?(twitter_accounts_path)}") %>
        <%= link_to("Tweets", tweets_path, class: "nav-link #{'active' if current_page?(tweets_path)}") %>
      </div>
      <div class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if Current.user %>
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= Current.user.email %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
              <li><%= link_to("Edit Password", edit_password_path, class: "dropdown-item") %></li>
              <li><%= button_to("Logout", logout_path, method: :delete, class: "dropdown-item") %></li>
            </ul>
          </div>
        <% else %>
          <%= link_to("Sign Up", sign_up_path, class: "nav-link #{'active' if current_page?(sign_up_path)}") %>
          <%= link_to("Login", sign_in_path, class: "nav-link #{'active' if current_page?(sign_in_path)}") %>
        <% end %>
      </div>
    </div>
  </div>
</nav>